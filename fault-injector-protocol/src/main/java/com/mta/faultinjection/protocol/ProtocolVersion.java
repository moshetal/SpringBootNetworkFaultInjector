package com.mta.faultinjection.protocol;

/** Negotiated wire protocol version between agents and the control server. */
public final class ProtocolVersion {

    public static final int CURRENT = 1;

    private ProtocolVersion() {}
}
